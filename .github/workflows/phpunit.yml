name: PHPUnit Tests
on: pull_request
jobs:
  phpunit:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: true
      matrix:
        php: [8.3, 8.4, 8.5]
        laravel: [12.*, 13.*]
        stability: [prefer-stable]
        include:
          - laravel: 12.*
            testbench: "^10.0"
          - laravel: 13.*
            testbench: "^11.0"
        exclude:
          - php: 8.3
            laravel: 13.*

    name: P${{ matrix.php }} - L${{ matrix.laravel }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          extensions: dom, curl, mbstring, pdo, bcmath, fileinfo, ldap
          coverage: none

      - name: Install dependencies
        uses: ramsey/composer-install@v4
        with:
          composer-options: "--no-cache --with=orchestra/testbench:${{ matrix.testbench }}"

      - name: List Installed Dependencies
        run: composer show -D

      - name: Execute tests
        run: composer run test
        env:
          RAY_ENABLED: false
